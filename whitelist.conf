
# ─── Vanguard Edge Proxy — Whitelisted IPs ───────────────────────────
# IPs listed here bypass rate limiting in the proxy.
# WAF inspection (SQLi/XSS) still applies to ALL IPs, including whitelisted.
# One IP per line. Lines starting with '#' are comments.
#
# NOTE: 127.0.0.1 is commented out so that simulate_attack.sh and
#       test.sh can demonstrate rate-limit blocking from localhost.
#       Uncomment it in production if needed.
# ─────────────────────────────────────────────────────────────────────

# 127.0.0.1
192.168.1.50
10.0.0.1